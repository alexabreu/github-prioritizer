import { Theme } from "@material-ui/core";

const styles = (theme: Theme) => {
  return {
    root: {
    },
    contentContainer: {
      display: 'flex',
      margin: '75px 0 0 0',
      border: '1px solid #eee',
      minHeight: '100vh',
    }
  };
}

export default styles;
