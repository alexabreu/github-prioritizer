import { Theme } from "@material-ui/core";

const styles = (theme: Theme) => {
  return {
    root: {
    },
    contentContainer: {
      display: 'flex',
      margin: '75px 0 0 0',
      border: '1px solid #eee',
    }
  };
}

export default styles;
